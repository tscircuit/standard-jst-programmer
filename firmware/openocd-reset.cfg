# Five-pin connector; target NRST must have its own 3.3 V pull-up.
source [find interface/cmsis-dap.cfg]
transport select swd
adapter speed 1000
reset_config srst_only srst_nogate connect_assert_srst
